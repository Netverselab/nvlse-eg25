[build]
command = "npm run build"
publish = "dist"

[[redirects]]
from = "/api/*"
to = "/.netlify/functions/api/:splat"
status = 200

[functions]
directory = "functions"
node_bundler = "esbuild"

[build.environment]
NODE_VERSION = "18"