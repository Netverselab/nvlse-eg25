{
  "name": "brave-search-engine",
  "version": "1.0.0",
  "description": "A real-time search engine using Brave API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "mkdir -p dist && cp -r public/* dist/ && uglifyjs public/index.html -o dist/index.html --compress --mangle"
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5",
    "axios": "^1.6.2",
    "express-rate-limit": "^7.1.5",
    "uglify-js": "^3.17.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}