{
  "name": "brave-search-engine",
  "version": "1.0.0",
  "description": "A real-time search engine using Brave API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "mkdir -p build && cp -r public/* build/ && cp -r functions build/",
    "postinstall": "npm install --prefix functions"
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5",
    "axios": "^1.6.2",
    "express-rate-limit": "^7.1.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}